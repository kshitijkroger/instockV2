<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Information</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        .container {
            border: 1px solid #ddd;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .title {
            font-size: 24px;
            font-weight: bold;
            margin-bottom: 20px;
        }
        .info {
            margin-bottom: 10px;
        }
        .info span {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="title">Incident Details</div>
        <div class="info"><span>Number:</span> <%= number %></div>
        <div class="info"><span>State:</span> <%= state %></div>
        <div class="info"><span>Configuration Item:</span> <%= cmdb_ci.display_value %></div>
        <div class="info"><span>Priority:</span> <%= priority %></div>
        <div class="info"><span>Short Description:</span> <%= short_description %></div>
        <div class="info"><span>Assigned To:</span> <%= assigned_to.display_value %></div>
        <div class="info"><span>Opened At:</span> <%= opened_at %></div>
        <div class="info"><span>Reported User:</span> <%= caller_id.display_value %></div>
        <div class="info"><span>Assignment Group:</span> <%= assignment_group.display_value %></div>
        <div class="info"><span>Store:</span> <%= location.display_value %></div>
    </div>
</body>
</html>
